Minibuffer API
#+begin_src elisp
  minibuffer-setup-hook
  minibuffer-with-setup-hooks  ;; want a one-off use
  post-command-hook
  after-change-functions
  call-interactively
#+end_src

M-x:
#+begin_src elisp
  execute-extended-command
  read-exteded-command
  completing-read
  INITIAL_INPUT
#+end_src


Company
#+begin_src
#+end_src

Pending to category
#+begin_src elisp
  eval-when-comple
  concat
  with-demoted-errors
  aw-switch-to-window
  aw-select
  call-interactively
  symbol-name
  symbol-function
#+end_src
